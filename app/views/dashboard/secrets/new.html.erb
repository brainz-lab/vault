<% content_for :title, "New Secret" %>

<div class="mx-auto max-w-2xl">
  <h1 class="text-2xl font-semibold text-gray-900">New Secret</h1>
  <p class="mt-2 text-sm text-gray-700">Create a new encrypted secret for <%= @environment.name %></p>

  <%= render "form", secret: @secret %>
</div>

<% content_for :title, "New Secret" %>

<div class="mx-auto max-w-2xl">
  <div class="page-header">
    <h1 class="page-title">New Secret</h1>
    <p class="page-subtitle">Create a new encrypted secret for <%= @environment.name %></p>
  </div>

  <%= render "form", secret: @secret %>
</div>

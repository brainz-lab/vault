<% content_for :title, "Edit #{@secret.key}" %>

<div class="mx-auto max-w-2xl">
  <h1 class="text-2xl font-semibold text-gray-900">Edit Secret</h1>
  <p class="mt-2 text-sm text-gray-700">Update <%= @secret.key %> in <%= @environment.name %></p>

  <%= render "form", secret: @secret %>
</div>

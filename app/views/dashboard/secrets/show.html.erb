<% content_for :title, @secret.key %>

<div class="max-w-6xl">
  <div class="sm:flex sm:items-center sm:justify-between mb-8">
    <div class="flex items-center">
      <div class="flex h-12 w-12 items-center justify-center rounded-xl <%= @secret.otp_enabled? ? 'bg-gradient-to-br from-emerald-100 to-emerald-200' : (@secret.credential? ? 'bg-gradient-to-br from-blue-100 to-blue-200' : '') %>" style="<%= @secret.otp_enabled? || @secret.credential? ? '' : 'background: linear-gradient(135deg, var(--color-vault-bg) 0%, var(--color-cream-200) 100%);' %>">
        <% if @secret.otp_enabled? %>
          <svg class="h-6 w-6 text-emerald-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        <% elsif @secret.credential? %>
          <svg class="h-6 w-6 text-blue-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 6a3.75 3.75 0 11-7.5 0 3.75 3.75 0 017.5 0zM4.501 20.118a7.5 7.5 0 0114.998 0A17.933 17.933 0 0112 21.75c-2.676 0-5.216-.584-7.499-1.632z" />
          </svg>
        <% else %>
          <svg class="h-6 w-6" style="color: var(--color-vault);" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 5.25a3 3 0 013 3m3 0a6 6 0 01-7.029 5.912c-.563-.097-1.159.026-1.563.43L10.5 17.25H8.25v2.25H6v2.25H2.25v-2.818c0-.597.237-1.17.659-1.591l6.499-6.499c.404-.404.527-1 .43-1.563A6 6 0 1121.75 8.25z" />
          </svg>
        <% end %>
      </div>
      <div class="ml-4">
        <div class="flex items-center gap-2">
          <h1 class="section-header font-mono"><%= @secret.key %></h1>
          <% if @secret.otp_enabled? %>
            <span class="badge bg-emerald-100 text-emerald-700 text-xs"><%= @secret.secret_type == "hotp" ? "HOTP" : "TOTP" %></span>
          <% elsif @secret.credential? %>
            <span class="badge bg-blue-100 text-blue-700 text-xs">Credential</span>
          <% end %>
        </div>
        <p class="section-subheader"><%= @secret.description || "No description" %></p>
      </div>
    </div>
    <div class="mt-4 sm:mt-0 flex gap-3">
      <%= link_to history_dashboard_project_secret_path(@current_project, @secret), class: "btn-secondary" do %>
        <svg class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        History
      <% end %>
      <%= link_to edit_dashboard_project_secret_path(@current_project, @secret), class: "btn-primary" do %>
        <svg class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M16.862 4.487l1.687-1.688a1.875 1.875 0 112.652 2.652L10.582 16.07a4.5 4.5 0 01-1.897 1.13L6 18l.8-2.685a4.5 4.5 0 011.13-1.897l8.932-8.931zm0 0L19.5 7.125M18 14v4.75A2.25 2.25 0 0115.75 21H5.25A2.25 2.25 0 013 18.75V8.25A2.25 2.25 0 015.25 6H10" />
        </svg>
        Edit
      <% end %>
    </div>
  </div>

  <!-- Environment Badge -->
  <div class="mb-8">
    <span class="badge <%= @environment.locked? ? 'badge-error' : 'badge-success' %> inline-flex items-center">
      <svg class="h-3.5 w-3.5 mr-1.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <% if @environment.locked? %>
          <path stroke-linecap="round" stroke-linejoin="round" d="M16.5 10.5V6.75a4.5 4.5 0 10-9 0v3.75m-.75 11.25h10.5a2.25 2.25 0 002.25-2.25v-6.75a2.25 2.25 0 00-2.25-2.25H6.75a2.25 2.25 0 00-2.25 2.25v6.75a2.25 2.25 0 002.25 2.25z" />
        <% else %>
          <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 10.5V6.75a4.5 4.5 0 119 0v3.75M3.75 21.75h10.5a2.25 2.25 0 002.25-2.25v-6.75a2.25 2.25 0 00-2.25-2.25H3.75a2.25 2.25 0 00-2.25 2.25v6.75a2.25 2.25 0 002.25 2.25z" />
        <% end %>
      </svg>
      <%= @environment.name %>
    </span>
  </div>

  <div class="grid gap-8 lg:grid-cols-3">
    <!-- Value Section -->
    <div class="lg:col-span-2 space-y-6">
      <div class="card">
        <div class="card-body">
          <div class="flex items-center justify-between mb-4">
            <h2 class="text-lg font-semibold" style="color: var(--color-ink-900);">Secret Value</h2>
            <span class="badge badge-neutral">v<%= @secret.current_version_number %></span>
          </div>

          <div data-controller="secret-reveal">
            <div class="relative">
              <div class="secret-display" data-secret-reveal-target="container">
                <span data-secret-reveal-target="hidden" class="secret-hidden">
                  ••••••••••••••••••••••••••••••••
                </span>
                <span data-secret-reveal-target="value" class="secret-revealed hidden"><%= @value %></span>
              </div>
              <div class="absolute top-3 right-3 flex gap-2">
                <button type="button" class="rounded-md bg-slate-700 px-2.5 py-1.5 text-xs font-medium text-slate-300 hover:bg-slate-600 transition-colors" data-action="click->secret-reveal#toggle" data-secret-reveal-target="button">
                  Show
                </button>
                <button type="button" class="rounded-md bg-slate-700 px-2.5 py-1.5 text-xs font-medium text-slate-300 hover:bg-slate-600 transition-colors" data-action="click->secret-reveal#copy" data-clipboard-text="<%= @value %>">
                  Copy
                </button>
              </div>
            </div>
            <p class="mt-3 text-xs flex items-center" style="color: var(--color-ink-500);">
              <svg class="h-3.5 w-3.5 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M2.036 12.322a1.012 1.012 0 010-.639C3.423 7.51 7.36 4.5 12 4.5c4.638 0 8.573 3.007 9.963 7.178.07.207.07.431 0 .639C20.577 16.49 16.64 19.5 12 19.5c-4.638 0-8.573-3.007-9.963-7.178z" />
                <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              </svg>
              Click "Show" to reveal the secret value
            </p>
          </div>
        </div>
      </div>

      <% if @secret.credential? || @secret.otp_enabled? %>
        <!-- Credential Info -->
        <div class="card">
          <div class="card-body">
            <div class="flex items-center justify-between mb-4">
              <h2 class="text-lg font-semibold" style="color: var(--color-ink-900);">Credential</h2>
              <span class="badge badge-primary"><%= @secret.secret_type.titleize %></span>
            </div>

            <dl class="space-y-4">
              <% if @secret.username.present? %>
                <div>
                  <dt class="text-xs font-medium uppercase tracking-wide" style="color: var(--color-ink-500);">Username</dt>
                  <dd class="mt-1 text-sm font-mono flex items-center justify-between" style="color: var(--color-ink-900);">
                    <%= @secret.username %>
                    <button type="button" class="text-xs font-medium px-2 py-1 rounded" style="color: var(--color-primary-600); background: var(--color-primary-50);" data-clipboard-text="<%= @secret.username %>" onclick="navigator.clipboard.writeText(this.dataset.clipboardText); this.textContent='Copied!'; setTimeout(() => this.textContent='Copy', 1500);">
                      Copy
                    </button>
                  </dd>
                </div>
              <% end %>
            </dl>
          </div>
        </div>
      <% end %>

      <% if @secret.otp_enabled? %>
        <!-- OTP Display -->
        <div class="card" data-controller="otp-display" data-otp-display-url-value="<%= generate_otp_dashboard_project_secret_path(@current_project, @secret) %>" data-otp-display-type-value="<%= @secret.secret_type %>" data-otp-display-period-value="<%= @secret.otp_period || 30 %>">
          <div class="card-body">
            <div class="flex items-center justify-between mb-4">
              <h2 class="text-lg font-semibold" style="color: var(--color-ink-900);">OTP Code</h2>
              <div class="flex items-center gap-2">
                <% if @secret.secret_type != "hotp" %>
                  <div class="flex items-center gap-2">
                    <div class="h-2 w-2 rounded-full animate-pulse" data-otp-display-target="indicator" style="background: var(--color-emerald-500);"></div>
                    <span class="text-xs" style="color: var(--color-ink-500);" data-otp-display-target="countdown">--s</span>
                  </div>
                <% end %>
                <span class="badge badge-success"><%= @secret.secret_type == "hotp" ? "Counter-based" : "Time-based" %></span>
              </div>
            </div>

            <div class="relative">
              <div class="font-mono text-4xl font-bold tracking-[0.3em] text-center py-6 rounded-xl" style="background: var(--color-ink-900); color: var(--color-cream-50);" data-otp-display-target="code">
                ------
              </div>
              <% if @secret.secret_type != "hotp" %>
                <div class="absolute bottom-0 left-0 h-1 rounded-full transition-all duration-1000" style="background: var(--color-primary-500);" data-otp-display-target="progress"></div>
              <% end %>
            </div>

            <div class="mt-4 flex gap-3">
              <button type="button" class="btn-secondary flex-1 justify-center" data-action="click->otp-display#copy">
                <svg class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M15.666 3.888A2.25 2.25 0 0013.5 2.25h-3c-1.03 0-1.9.693-2.166 1.638m7.332 0c.055.194.084.4.084.612v0a.75.75 0 01-.75.75H9a.75.75 0 01-.75-.75v0c0-.212.03-.418.084-.612m7.332 0c.646.049 1.288.11 1.927.184 1.1.128 1.907 1.077 1.907 2.185V19.5a2.25 2.25 0 01-2.25 2.25H6.75A2.25 2.25 0 014.5 19.5V6.257c0-1.108.806-2.057 1.907-2.185a48.208 48.208 0 011.927-.184" />
                </svg>
                Copy Code
              </button>
              <% if @secret.secret_type == "hotp" %>
                <button type="button" class="btn-primary flex-1 justify-center" data-action="click->otp-display#refresh">
                  <svg class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M16.023 9.348h4.992v-.001M2.985 19.644v-4.992m0 0h4.992m-4.993 0l3.181 3.183a8.25 8.25 0 0013.803-3.7M4.031 9.865a8.25 8.25 0 0113.803-3.7l3.181 3.182m0-4.991v4.99" />
                  </svg>
                  Generate Next
                </button>
              <% end %>
            </div>

            <p class="mt-3 text-xs text-center" style="color: var(--color-ink-500);">
              <% if @secret.secret_type == "hotp" %>
                Counter-based OTP - click "Generate Next" to increment the counter
              <% else %>
                Time-based OTP - refreshes every <%= @secret.otp_period || 30 %> seconds
              <% end %>
            </p>
          </div>
        </div>
      <% end %>

      <!-- Recent Versions -->
      <div class="card overflow-hidden">
        <div class="px-6 py-4 flex justify-between items-center" style="background: var(--color-cream-50); border-bottom: 1px solid var(--color-cream-200);">
          <h2 class="text-lg font-semibold" style="color: var(--color-ink-900);">Recent Versions</h2>
          <%= link_to "View All", history_dashboard_project_secret_path(@current_project, @secret), class: "link text-sm font-medium" %>
        </div>
        <ul class="divide-y" style="border-color: var(--color-cream-100);">
          <% @versions.each do |version| %>
            <li class="px-6 py-4 flex items-center justify-between">
              <div>
                <p class="text-sm font-medium" style="color: var(--color-ink-900);">Version <%= version.version %></p>
                <p class="text-sm" style="color: var(--color-ink-500);">
                  <%= version.created_at.strftime("%b %d, %Y at %H:%M") %>
                  <% if version.change_note.present? %>
                    - <span class="italic"><%= version.change_note %></span>
                  <% end %>
                </p>
              </div>
              <% if version.version != @secret.current_version_number %>
                <%= button_to rollback_dashboard_project_secret_path(@current_project, @secret, version: version.version),
                    method: :post,
                    data: { confirm: "Roll back to version #{version.version}?" },
                    class: "link text-sm font-medium" do %>
                  Rollback
                <% end %>
              <% else %>
                <span class="badge badge-success">Current</span>
              <% end %>
            </li>
          <% end %>
          <% if @versions.empty? %>
            <li class="px-6 py-10 text-center text-sm" style="color: var(--color-ink-500);">No versions in this environment</li>
          <% end %>
        </ul>
      </div>
    </div>

    <!-- Metadata Section -->
    <div class="space-y-6">
      <%# Quick copy card for credentials %>
      <%= render "dashboard/secrets/credential_card", secret: @secret, environment: @environment, value: @value, project: @current_project %>

      <div class="card">
        <div class="card-body">
          <h2 class="text-lg font-semibold mb-4" style="color: var(--color-ink-900);">Details</h2>
          <dl class="space-y-4">
            <div>
              <dt class="text-xs font-medium uppercase tracking-wide" style="color: var(--color-ink-500);">Path</dt>
              <dd class="mt-1 text-sm font-mono" style="color: var(--color-ink-900);"><%= @secret.path %></dd>
            </div>
            <div>
              <dt class="text-xs font-medium uppercase tracking-wide" style="color: var(--color-ink-500);">Type</dt>
              <dd class="mt-1 text-sm" style="color: var(--color-ink-900);"><%= @secret.secret_type&.titleize || "String" %></dd>
            </div>
            <% if @secret.secret_folder %>
              <div>
                <dt class="text-xs font-medium uppercase tracking-wide" style="color: var(--color-ink-500);">Folder</dt>
                <dd class="mt-1 text-sm" style="color: var(--color-ink-900);"><%= @secret.secret_folder.path %></dd>
              </div>
            <% end %>
            <% if @secret.otp_enabled? %>
              <div>
                <dt class="text-xs font-medium uppercase tracking-wide" style="color: var(--color-ink-500);">OTP Config</dt>
                <dd class="mt-1 text-sm" style="color: var(--color-ink-900);">
                  <%= @secret.otp_digits || 6 %> digits /
                  <%= (@secret.otp_algorithm || "sha1").upcase %> /
                  <%= @secret.otp_period || 30 %>s
                </dd>
              </div>
              <% if @secret.otp_issuer.present? %>
                <div>
                  <dt class="text-xs font-medium uppercase tracking-wide" style="color: var(--color-ink-500);">OTP Issuer</dt>
                  <dd class="mt-1 text-sm" style="color: var(--color-ink-900);"><%= @secret.otp_issuer %></dd>
                </div>
              <% end %>
            <% end %>
            <div>
              <dt class="text-xs font-medium uppercase tracking-wide" style="color: var(--color-ink-500);">Created</dt>
              <dd class="mt-1 text-sm" style="color: var(--color-ink-900);"><%= @secret.created_at.strftime("%b %d, %Y") %></dd>
            </div>
            <div>
              <dt class="text-xs font-medium uppercase tracking-wide" style="color: var(--color-ink-500);">Last Updated</dt>
              <dd class="mt-1 text-sm" style="color: var(--color-ink-900);"><%= @secret.updated_at.strftime("%b %d, %Y at %H:%M") %></dd>
            </div>
            <% if @secret.rotation_interval_days %>
              <div>
                <dt class="text-xs font-medium uppercase tracking-wide" style="color: var(--color-ink-500);">Rotation Schedule</dt>
                <dd class="mt-1 text-sm" style="color: var(--color-ink-900);">Every <%= @secret.rotation_interval_days %> days</dd>
              </div>
            <% end %>
          </dl>
        </div>
      </div>

      <% if @secret.tags.present? %>
        <div class="card">
          <div class="card-body">
            <h2 class="text-lg font-semibold mb-4" style="color: var(--color-ink-900);">Tags</h2>
            <div class="flex flex-wrap gap-2">
              <% @secret.tags.each do |key, value| %>
                <span class="badge badge-warning">
                  <%= key %>: <%= value %>
                </span>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>

      <!-- Danger Zone -->
      <div class="card border-red-200">
        <div class="card-body">
          <h2 class="text-lg font-semibold text-red-600 mb-3 flex items-center">
            <svg class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126zM12 15.75h.007v.008H12v-.008z" />
            </svg>
            Danger Zone
          </h2>
          <p class="text-sm mb-4" style="color: var(--color-ink-500);">Archiving a secret will hide it from the list but preserve its history.</p>
          <%= button_to dashboard_project_secret_path(@current_project, @secret),
              method: :delete,
              data: { confirm: "Are you sure you want to archive this secret?" },
              class: "btn-danger w-full justify-center" do %>
            <svg class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M20.25 7.5l-.625 10.632a2.25 2.25 0 01-2.247 2.118H6.622a2.25 2.25 0 01-2.247-2.118L3.75 7.5m6 4.125l2.25 2.25m0 0l2.25 2.25M12 13.875l2.25-2.25M12 13.875l-2.25 2.25M3.375 7.5h17.25c.621 0 1.125-.504 1.125-1.125v-1.5c0-.621-.504-1.125-1.125-1.125H3.375c-.621 0-1.125.504-1.125 1.125v1.5c0 .621.504 1.125 1.125 1.125z" />
            </svg>
            Archive Secret
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

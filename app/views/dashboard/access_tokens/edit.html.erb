<% content_for :title, "Edit #{@token.name}" %>

<div class="mx-auto max-w-2xl">
  <div class="page-header">
    <h1 class="page-title">Edit Access Token</h1>
    <p class="page-subtitle">Update settings for <%= @token.name %></p>
  </div>

  <%= render "form", token: @token %>
</div>

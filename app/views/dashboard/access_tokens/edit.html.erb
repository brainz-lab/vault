<% content_for :title, "Edit #{@token.name}" %>

<div class="mx-auto max-w-2xl">
  <h1 class="text-2xl font-semibold text-gray-900">Edit Access Token</h1>
  <p class="mt-2 text-sm text-gray-700">Update token settings</p>

  <%= render "form", token: @token %>
</div>

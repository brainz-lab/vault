<% content_for :title, "New Environment" %>

<div class="mx-auto max-w-2xl">
  <div class="mb-8">
    <h1 class="section-header">New Environment</h1>
    <p class="section-subheader">Create a new environment for secrets</p>
  </div>

  <%= render "form", environment: @environment %>
</div>

<% content_for :title, "New Environment" %>

<div class="mx-auto max-w-2xl">
  <h1 class="text-2xl font-semibold text-gray-900">New Environment</h1>
  <p class="mt-2 text-sm text-gray-700">Create a new environment for secrets</p>

  <%= render "form", environment: @environment %>
</div>

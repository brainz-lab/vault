<% content_for :title, "Edit #{@environment.name}" %>

<div class="mx-auto max-w-2xl">
  <h1 class="text-2xl font-semibold text-gray-900">Edit Environment</h1>
  <p class="mt-2 text-sm text-gray-700">Update environment settings</p>

  <%= render "form", environment: @environment %>
</div>
